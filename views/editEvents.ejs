<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="/js/events.js" defer></script>
    <link rel="stylesheet" type="text/css" href="/styles.css" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>Edit Events</title>
  </head>
  <body>
    <header>
      <div class="bg-white h-[75px] flex items-center justify-center px-4 py-3">
        <img src="/images/LuminaFoundationLogo.svg" />
      </div>
    </header>
    <div class="flex">
      <div class="w-full">
        <div
          class="flex align-middle justify-center h-16 sm:h-14 font-bold p-2 py-6 mb-3 sm:mb-0"
        >
          <p class="text-lg">Edit Events</p>
        </div>

        <div id="form" class="bg-blue-900 hs-screen w-full sm:full pt-3">
          <div id="buttonContainer" class="flex flex-col justify-evenly">
            <a
              href="/admin/events"
              class="text-black bg-white flex items-center justify-center text-center mx-5 h-8 sm:h-10 my-5 sm:mx-20 font-semibold rounded hover:bg-blue-600 hover:text-white duration-200"
            >
              Back</a
            >
            <div
              class="flex items-center justify-center text-center font-bold text-white pb-2 w-full"
            >
              <form id="editEventsForm" name="editEventsForm" class="w-full">
                <fieldset>
                  <div class="bg-white sm:mx-20 p-2 m-5 rounded h-30">
                    <input
                      type="text"
                      name="title"
                      class="w-full text-black focus:outline-none"
                      value="<%= initialData.title %>"
                      required
                    />
                  </div>
                  <div class="bg-white sm:mx-20 p-2 m-5 rounded h-110">
                    <textarea
                      rows="17"
                      name="body"
                      required
                      class="w-full text-black focus:outline-none text-wrap resize-none"><%=initialData.body%></textarea
                    >
                  </div>

                  <% if (initialData.imageurl) { %>
                    Current Image:
                    <div class = "flex items-center justify-center p-2 m-5">
                    
                      <img src="<%= initialData.imageurl %>" class = "flex items-center justify-center" alt="Current Image" style="max-width: 400px; max-height: 400px;" />
                    </div>
                  <% } %>
                  
                  <input
                    type="file"
                    id="myfile"
                    name="myfile"
                    accept=".png, .jpg, .jpeg"
                  />

              
                  <% if (initialData.dateTime) { %>
                    <div class="bg-white sm:mx-20 p-2 m-5 rounded h-125 sm:h-100">
                      <p class="text-black">
                        Date: <%= new Date(initialData.dateTime).toLocaleString('default', {month: 'long'}) %> <%= new Date(initialData.dateTime).getDate() %>, <%= new Date(initialData.dateTime).getFullYear() %>
                      </p>
                      <input
                        type="datetime-local"
                        name="date"
                        class="text-black sm:full"
                        required
                      />
                    </div>
                    <% } else { %>
                    <div class="bg-white sm:mx-20 p-2 m-5 rounded h-125 sm:h-100">
                      <input
                        type="datetime-local"
                        name="date"
                        class="text-black sm:full"
                        required
                      />
                    </div>
                    <% } %>

                  <div
                    style="cursor: pointer"
                    class="text-black bg-white flex items-center justify-center text-center mx-5 h-16 sm:h-20 my-5 sm:mx-20 font-semibold rounded hover:bg-blue-600 hover:text-white duration-200"
                  > 
                    <button
                      id="confirmeditEvents"
                      type="submit"
                      class="w-full h-full"
                    >
                      Confirm Edit
                    </button>
                  </div>
                </fieldset>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>

    <style is:global>
      * {
      }
      img {
        content-visibility: auto;
      }
      body {
        font-family: "Inter", sans-serif;
        font-size: 16px;
        line-height: normal;
        background-color: #000000;
        background: #f5f5f5;
        overflow-x: hidden;
      }
    </style>
  </body>
</html>
